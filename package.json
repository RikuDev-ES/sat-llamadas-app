{
  "name": "sat-llamadas-app",
  "version": "1.0.1",
  "description": "Aplicación de escritorio para registrar llamadas del SAT",
  "main": "frontend/main.js",
  "author": "SAT",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"cd backend && python app.py\" \"electron .\"",
    "build:backend": "cd backend && \"C:\\Users\\RIKUDEV\\AppData\\Local\\Programs\\Python\\Python312\\python.exe\" -m PyInstaller --onefile --name backend --distpath dist --noconfirm app.py",
    "build:electron": "node scripts/package-electron.cjs",
    "build:copy-backend": "node scripts/copy-backend.cjs",
    "build": "npm run build:backend && npm run build:electron && npm run build:copy-backend"
  },
  "keywords": [
    "electron",
    "sat",
    "llamadas",
    "flask"
  ],
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "concurrently": "^8.2.0",
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3",
    "electron-is-dev": "^2.0.0"
  }
}
